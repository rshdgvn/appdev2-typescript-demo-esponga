(() => {
    enum Role {
        // SuperAdmin,
        // SuperAdmin = 1,
        SuperAdmin = 'SuperAdmin',
        // Manager,
        Manager = 'Manager',
        // Staff,
        Staff = 'Staff',
    }

    // let userRole: Role = 0;
    let userRole: Role = SuperAdmin Role.SuperAdmin;
})